{% extends 'base.html.twig' %}

{% block title %}{{ 'page_title'|trans }} - {{ 'quellen'|trans }}{% endblock %}

{% block body %}
    <div class="jumbotron">
            <div class="card">
                <div class="card-header">
                    {{ 'quellen'|trans }}
                </div>
                <div class="card-body">
                    <div class="accordion" id="sources">
                        {{ include('defaults/quellen/addresses.html.twig') }}
                        {{ include('defaults/quellen/wahlkreise_berlin.html.twig') }}
                        {{ include('defaults/quellen/wahllokale_berlin_23.html.twig') }}
                        {{ include('defaults/quellen/btw.html.twig') }}
                        {{ include('defaults/quellen/ltw.html.twig') }}
                    </div>
                </div>
            </div>
    </div>
{% endblock %}
