{% extends 'base.html.twig' %}

{% block title %}{{'page_title'|trans}} - {{ 'impressum'|trans }}{% endblock %}

{% block body %}
    <div class="jumbotron">
        <div class="row">
            <div class="card">
                <div class="card-header">
                    {{ 'impressum'|trans }}
                </div>
                <div class="card-body">
                    {{ 'impress.body'|trans({}, 'terms')|raw }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
